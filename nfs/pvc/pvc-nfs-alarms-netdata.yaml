apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pi8s-nfs-csi-alarms-netdata-pvc
spec:
  storageClassName: nfs-csi-alarms-netdata
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi